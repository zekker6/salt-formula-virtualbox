{%- set host = salt['grains.filter_by']({
    'Debian': {
        'service': 'virtualbox',
        'version': '6.1',
    },
    'RedHat': {
        'service': 'virtualbox',
        'version': '6.1',
    },
}, merge=salt['pillar.get']('virtualbox:host')) %}